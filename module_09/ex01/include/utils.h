#ifndef UTILS_HPP
#define UTILS_HPP

#include <cstdlib>
#include <string>

std::string trim(const std::string &str);

bool validateInput(const std::string &str);

bool isOp(char *c);

#endif // !UTILS_HPP
